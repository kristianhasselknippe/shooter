var input = {
	left_down => false,
	right_down => fale,
	up_down => false,
	down_down => false
}

var Vector2 = {
	x => 0,
	y => 0,
}

var GameObject = {
	pos => $new(Vector2),
	name => "unnamed",
}

$exports.set_input = function(left,top,right,down) {
	$print("Setting input: " + left + ", " + top + ", " + right + ", " + down);
}

var a_game_object = $new(GameObject)

$exports.update = function() {
    $print("hello neko world!!\n");
}